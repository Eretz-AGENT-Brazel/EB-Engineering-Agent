# ⭐ API+KNOWLEDGE-DEVELOP — נתיב פיתוח 1: ה-API והידע ההנדסי

**כאן נבנה הסוכן.** התיקייה הזו מרכזת את כל בניין הידע ההנדסי ואת כל בניית ידע התוכנה —
מהשיעור הראשון ועד ההטמעה. היעד, כפי שאמיר הגדיר (12/08/2026): מכאן ייבנה **המוצר** —
הקונסולה/פלאגין שיוטמע רשמית במחשבי החברה, סוכן שיושב בתוך התוכנה, ממדל ופותר בעיות
הנדסיות. וגם אחרי העלייה לאוויר — כל השיפורים והעדכונים יבוצעו כאן.

*(נתיב פיתוח 2 — ידיעת התקנים — יושב ב-`standards/` בשורש הריפו, נעול עד הכרזת אמיר.)*

⚠️ **מוסכמת נתיבים:** כל נתיב במסמכים שבתוך התיקייה הזו (`app/...`, `knowledge/...`,
`projects/...`) הוא **יחסי לתיקייה הזו**, אלא אם כתוב אחרת.

---

## 📁 מה יש כאן

| תיקייה | מה בפנים | מתי הולכים לשם |
|---|---|---|
| **`app/`** | הקוד החי: `eb_api.py` (הגשר לתוסף — **הרשות היחידה לגרסה**), `plugin/` (התוסף: מקורות + DLL + ערוץ הפקודות), `acad.py` (‏COM), כלי האימות (`vfy_joined.py`, ‏`audit.py`), הקונסולה (`console.py`, מוקפאת) | כל הפעלה של התוכנה מקוד |
| **`qc/`** | השומרים: `consistency.py` (חוסם קומיט), `selftest_consistency.py` (מוכיח שהשומר חי), ‏`retracted.tsv` (טענות שהופרכו), ‏`built-legacy.tsv` + `joints-legacy.tsv` (פיגור מוצהר — **רק מתכווצות**) | לפני כל קומיט |
| **`knowledge/`** | הידע הנצבר: `learning/manual/` (הערות פרקי המדריך A/B/D/E), ‏`learning/findings/` (‏THE CEILING · LETHAL CALLS · קטלוגי חתכים), ‏`learning/audits/` (ביקורות פרקים), ‏`api/` (מפת ה-API מרפלקציה), ‏`steel/` (ידע פלדה) | כשלומדים או בודקים עובדה |
| **`lessons/`** | שיעורים 1–5 עם אמיר: יומני שיעור, לקחים, רטרוספקטיבות, וב-`audits/` — ביקורות Fable והנחיות Opus מאותה תקופה | היסטוריית הלמידה מאמיר |
| **`projects/`** | המודלים: ‏`SANDBOX/` (מודלי הלימוד של פרקים A–E + ‏`sandbox.dwg` **של אמיר — לא לגעת**), תיקיות `שיעור-1..5` עם יומני הבנייה | כשפותחים או קוראים מודל |
| **`research/`** | מחקרים הנדסיים — מחקר הביסוס והעיגון של עמודי פלדה (סוף שיעור 5): 6 מסמכים, דוח HTML ו-PDF | ‏value engineering, פרטי עיגון |
| **`data/`, `assets/`** | מצב הקונסולה (פרויקטים, למידה) + אייקונים | רק דרך הקונסולה |

## 🕰️ ציר הזמן של הנתיב (איך הגענו לכאן)

1. **שיעורים 1–3** — למידה דרך מודלים קיימים: צפייה באמיר, העתקת מודל, בנייה מאפס.
2. **שיעורים 4–5** — שיעורים שאמיר העביר: עמוד + פלטת בסיס, עיגון לקיר ולרצפה. ציון 90/100 במבחן.
3. **מחקר הביסוס** (סוף שיעור 5) — ‏`research/`.
4. **למידת המדריך פרק-פרק** (מ-09/08) — ‏A ‏6/6 ✅ · B ‏29/33 · D ‏7/7 ✅ · E ‏11/11 ✅ · C ‏0/22.
   המעקב המלא: `../PROGRESS.md` (בשורש).

## 🚦 שלושה כללים שאין לשכוח

1. לפני כל קומיט: `python qc/consistency.py` (מכאן) חייב להדפיס **CLEAN**, ואחרי קומיט — פוש.
2. גרסת התוסף נקבעת **רק** ב-`app/eb_api.py`. כל מספר אחר הוא ציטוט מתוארך.
3. ⛔ לפני כל ניסיון API חדש: ‏`knowledge/learning/findings/LETHAL-CALLS-do-not-invoke.md`
   ו-`THE-CEILING-what-code-cannot-reach.md`.

<div dir="rtl" align="right">

# 🗄️ ארכיון — `Desktop\.claude` (נשמר 17/08/2026)

*התוכן של `C:\Users\User\Desktop\.claude` כפי שהיה ברגע הארכוב, לפני שהתיקייה נמחקה
משולחן העבודה בהוראת אמיר.*

---

## למה זה כאן

אמיר שאל אם אפשר למחוק את `Desktop\.claude`. הבדיקה העלתה ש**אף אחד מתכולתה אינו קריטי
ל-EB PROSTEEL AGENT** — לריפו הזה אין בכלל תיקיית `.claude` משלו — **אבל** תיקיית
`portal/` הייתה **העותק היחיד בעולם** של תוכנה עובדת: שולחן העבודה אינו ריפו גיט, וסריקה
על כל פרופיל המשתמש החזירה תוצאה אחת בלבד. לכן: **מארכבים ואז מוחקים**, ולא מוחקים ישר.

⚠️ **אין לבלבל עם `C:\Users\User\.claude`** (תיקיית הבית) — שם יושבים **הסקיל החי**
`prosteel-modeling` ו**הזיכרון**, והיא לא נגעה ולא תיגע.

## אימות ההעתקה (נמדד, לא מוצהר)

| שער | תוצאה |
|---|---|
| ‏`diff -r` מקור מול ארכיון | **שתק — אפס הבדלים** |
| ספירת קבצים | ‏16 מול 16 |
| סך בתים | ‏57,202 מול 57,202 |

## מה יש כאן

| מה | תיאור |
|---|---|
| **`portal/`** | ‏**EB AI — Mission Control**: פורטל ניהול מקומי לכל תוכנית ה-AI של ארץ ברזל. נבנה ואומת ב-17/06/2026 (שלבים 1–3 מתוך 4). ‏`server.py` (‏stdlib `http.server` + API של JSON) · ‏`app/` (‏SPA בוונילה עם מרנדר Markdown משלו) · ‏`data/*.json` (8 אוספים) · ‏`Portal.bat` (הפעלה בקליק). המפרט המלא: `portal/PLAN.md` |
| **`launch.json`** | הגדרות שרתי הפיתוח של שולחן העבודה: `tankforge` (פורט 8123, מגיש `TankForge/app`) ו-`portal` (8190) |
| **`settings.local.json`** | רשימת ההרשאות שהצטברה לתיקיית העבודה `C:\Users\User\Desktop` לאורך ~חודשיים |

## מצב הפורטל בזמן הארכוב — למה הוא נזנח

הפורטל **עבד**, אבל נטוש בפועל: קובצי `data/*.json` לא נגעו מ-**17/06/2026**, וכל ניהול
הפרויקט מאז עבר למסמכים החיים בריפו הזה — `SESSION-START.md` · `PROGRESS.md` ·
‏`RESUME-HERE.md`. כלומר הפונקציה שהפורטל נועד למלא ("המסך חד-פעמי, הפורטל הוא הזיכרון")
**מולאה בדרך אחרת**, והפורטל נשמר כנכס ולא כמערכת חיה.

## איך מחזירים לחיים (אם אי פעם)

```bash
cp -r "z-archive/desktop-dot-claude-2026-08-17/portal" "/c/Users/User/Desktop/.claude/"
cp "z-archive/desktop-dot-claude-2026-08-17/launch.json" "/c/Users/User/Desktop/.claude/"
```

ואז `Portal.bat` בקליק, או `preview_start` על התצורה `portal` ← ‏http://localhost:8190.

⚠️ **מלכודת מתועדת מהבנייה המקורית:** ‏`app.js` ו-`style.css` נושאים `?v=N` ב-`index.html`
(השרת אינו שולח כותרות cache) — **מעלים את המספר בכל עריכה**, אחרת הדפדפן מגיש ישן.

## שחזור ההרשאות בלבד

אם חזרו שאלות הרשאה מציקות אחרי המחיקה — זה מספיק, בלי להחזיר את הפורטל:

```bash
cp "z-archive/desktop-dot-claude-2026-08-17/settings.local.json" "/c/Users/User/Desktop/.claude/"
```

</div>

# 🔁 פרומפט העברה — **חלק B הושלם.** מה הלאה

*נכתב 10/08/2026 בסוף הסשן שהשלים B.20–B.29. מחליף את `HANDOFF-B20.md`, שסיים את תפקידו.*

---

## ⚠️⚠️ קודם כל — מאיפה לפתוח את הסשן

**לפתוח את קלוד קוד על `C:\Users\User\Desktop` — לא על תיקיית הפרויקט.**

30 הזיכרונות יושבים ב-`C:\Users\User\.claude\projects\C--Users-User-Desktop\memory\`, שמקודד
את ה-Desktop. פתיחה על `...\Desktop\EB PROSTEEL AGENT` מפנה לתיקייה אחרת **שאין בה `memory`
בכלל**. **בדיקה של 30 שניות:** אם הסוכן לא יודע מה כתוב ב-`MEMORY.md` — לעצור ולהגיד לאמיר.

---

## ✅ מה הושלם

**כל 29 הפרקים של חלק B עברו ביקורת.** `python qc/consistency.py` → `CLEAN`, 29/29 פרקים
מכוסים, לכל אחד סימון מתוארך בהערות שלו. הרשומה המלאה:
`knowledge/learning/audits/AUDIT-PART-B-2026-08-10.md`.

תוסף **v156 → v172** · המודל `B08-insert-shapes.dwg`, **1,194 ישויות**, שמור.
🧲 **`vfy_fit` על כל המודל: `BOLT-NO-HOLE=0`** — כלל הברזל נקי מקצה לקצה.

---

## ⏸️ ארבע החלטות — **בהולד מפורש של אמיר (11/08/2026)**

> **"4 ההחלטות האלו לא רלוונטיות כרגע. אגיד לך כשנחזור ונמשיך לעבוד."**
>
> ⛔ **אין לפתוח אותן מיוזמת הסוכן, ואין להציע אותן כמשימה הבאה.** הן מתועדות כאן כדי שהמדידה
> לא תלך לאיבוד — **לא כרשימת מטלות.** הן נפתחות כשאמיר אומר, ולא לפני.

| | מה | המספר |
|---|---|---|
| **1** | ⏳ **מספור מיקום** — הסכימה: תחיליות משפחה + סבולת ה**זווית** של עמוד/קורה | **0 מתוך 1,194** חלקים ממוספרים. חוסם את השכפול של B.4.5, את Compare+Modify של B.28.3 ואת זיהוי הקבוצות של B.29.1. ריצה יבשה: **677 חלקים → 272 מיקומים נבדלים**, 8.7 שניות |
| **2** | ⏳ **סכימת הקבוצות** — מלאי / משלוח / הרכבה באתר | **414 מתוך 707** חלקי פלדה אינם בשום קבוצה |
| **3** | ⏳ **פער 31 מ"מ ב-B.25** — להזיז את המוטות לציר ±110, או פלטת מילוי 31 מ"מ בכל אחד מארבעת הקצוות | **כל שמונת הברגים** במפרץ, לא אחד |
| **4** | ⏳ **32 ברגים `SHORT`** — לא רק ה-12 שנרשמו | 6 קצרים מהחבילה (spare −9), 6 עם 11 מ"מ, ועוד 20 עם spare 3 |
| **5** | ⏸️ סכימת מחלקות תצוגה / משפחה (B.5) | ללא שינוי |

⭐ **הדפוס, שהתייצב שלוש פעמים:** היכן שהקלט הוא **מוסכמה של בית מלאכה ולא גאומטריה** —
**למדוד ולדווח, לא להמציא.** היכולת מוכחת בכל השלושה; הסכימה שלו.

---

## ⛔ ארבע קריאות קטלניות — לקרוא לפני כל קריאה לא מוכרת

`knowledge/learning/findings/LETHAL-CALLS-do-not-invoke.md`

1. `PsPlate.computeObjectWeigth` · 2. `PsVolume.checkHoleEdgeDistance` ·
3. **`PsGrid.addUserXaxis`** על גריד שנקשר דרך `PsTransaction` · 4. **קשירה+קריאה של
`PsEditConnection`**

> ⭐⭐ **בטיחות היא לפי טיפוס, לא לפי פעולה.** B.23 הסיק *"קריאה מאובייקט קשור בטוחה, מוטציה
> חשודה"* על סמך שלושה טיפוסים. `PsEditConnection` הוא **קריאה** והוא קטלני.
> קריאה־בטוח **עד כה**: `PsGrid`, `PsGussetConnection`, `PsPlate`, `PsShape`. כל השאר —
> **לא ידוע, וכל תשובה עולה קריסה.**

**⭐ נוהל השחזור תוקן:** להעביר את **ה-DWG בשורת הפקודה**, לא `/t <template>` — תבנית יוצרת
`Drawing1` חדש, וזה מרים דיאלוג **"Measurement Unit"** מודאלי ש**אי אפשר לסגור מהקוד**
(`BM_CLICK` ו-`WM_COMMAND` שניהם התעלמו, שישה ניסיונות).
⚠️ **`Get-Process acad` אינו המבחן** — קריסה יכולה להשאיר את התהליך ברשימה ו-`Responding=True`.
**`modal_dialogs()` הוא המבחן.**

---

## 🧭 חמשת הכללים שהסשן הזה ייצר

> ⭐⭐ **1. קרא בחזרה את מה שנתת, לפני שאתה קובע שהקריאה מסרבת.** `insert()=False` אמר עשר פעמים
> "המוצר אינטראקטיבי". קריאה חוזרת של קו המערכת אמרה `(NaN,NaN,NaN)` — **הגאומטריה מעולם לא
> הגיעה.** `False` מספר לך שהקריאה נכשלה; לעולם לא שהיא הייתה תקינה.

> ⭐⭐ **2. שני כשלים שנראים זהים מבחוץ שונים מבפנים.** `PsBracing` לא מקבל את הערך;
> `PsHaunchLinkDataMgd` מקבל אותו במדויק **ומתעלם** ממנו. רק קריאה־חוזרת מבדילה.

> ⭐⭐ **3. השם של החלק אומר מה הוא בפועל.** `FL 150x10` = פלדה שטוחה מהקטלוג · `BRFL 160x15` =
> שטוחה רחבה · **`Plate 165x10` = לא קיים באף קטלוג, ייחתך מפלטה.** `key` ו-`cat` זהים בשלושתם.

> ⭐⭐ **4. אמת את המכשיר לפני שאתה מאמין למספר שלו.** `connverify` דיווח 32 דגלים — כולם שקריים.
> `grouporphans` נראה כמו מונה גלובלי שדולף — הצלבה מול `groupinfo` הראתה שהוא נכון.
> **החשד היה שגוי ולבדוק אותו היה נכון בכל זאת.**

> ⭐⭐ **5. ממצא אמיתי שלא נוסע שווה אפס, ואף שומר לא רואה אותו.** שלוש פעמים: ה-*"likely"* של
> B.19 שהפך לוודאי · כלל ההושבה של B.23 שנכתב ולא יושם על מפרץ שנבנה **באותו יום** · ה-NaN של
> B.24. **רשימת עצירה תופסת סתירות, לא שתיקה. רק קריאת הפרק השכן תופסת.**

---

## 🔧 מה נבנה שאפשר להשתמש בו מיד

| אופ | מה הוא נותן |
|---|---|
| **`bind handle= cls=grid\|gusset\|plate\|shape`** | ⭐⭐⭐ `PsTransaction.GetObject` — **57 עומסי-יתר**, בינדר גנרי כמעט לכל טיפוס ProSteel. **הבינדר אינו על המחלקה, הוא על הטרנזקציה.** ⚠️ **אינו בודק טיפוס** — מסרב בעצמו על אי-התאמה |
| **`grouporphans`** | `Check Groups` של B.28.3, מורכב משאילתות השייכות |
| **`posauto dry=1`** | מספור במצב בדיקה, 8.7 שניות על 677 חלקים, לא כותב דבר |
| **`connscan`** | ידיות במקום מצביעים · טיפוס מ-`lk.Type` · **405 קישורים ב-1,147 חלקים, כולם מסווגים** |
| **`splice boltstyle=<שם>`** | ⛔ **בלעדיו החיבור קודח ולא מבריג** — שתי התבניות נושאות `BoltStyleCRC=0` |
| **`shearplate` / `splice`** | שומר כלל ברזל: `EB_ERR` כשנוצרו פלטות ואפס ברגים |
| **`eb_api.dumpmodel()`** | ⚠️ `insert` הוא `(0,0,0)` לכל פלטה וכל בורג. **להשתמש ב-`center`** |

---

## מה שאמיר החליט ואסור לפתוח מחדש

⏸️ **שלב 2 נעול** (בחירת מחברים לפי עומס) · ⏸️ `Duebel.mdb` · ⏸️ כתיבה למסדי המחברים ·
⏸️ שלוש הצעות חלק A · ⛔ `connset` בהסגר · ⛔ LISP · מטרי תמיד · לשמור אחרי כל שלב ·
`sandbox.dwg` הוא של אמיר.

⚠️ **ולארץ ברזל אין תקן מחברים כתוב** — הכל מניסיון אמיר ולפי דרישת הפרויקט.
**אסור להמציא פרט ולקרוא לו "הסטנדרט של ארץ ברזל".**

---

## ⚙️ הפעלה

```powershell
Start-Process 'C:\Program Files\Autodesk\AutoCAD 2015\acad.exe' -ArgumentList `
  '"C:\Users\User\Desktop\EB PROSTEEL AGENT\projects\SANDBOX\B08-insert-shapes.dwg"', `
  '/p','"C:\Program Files\Bentley\ProStructures Ss6 R1\AutoCAD 2015\Prg\ProStructures_SS6.1ACAD_E001_409.arg"', `
  '/ld','ProStructuresLoader.arx' `
  -WorkingDirectory 'C:\Program Files\Bentley\ProStructures Ss6 R1\AutoCAD 2015\Dwg'
```

ואז `eb_api._netload()` · `run("ping")` · `run("list")`.
**גרסת התוסף — תמיד לפי `app/eb_api.py`**, לא לפי מספר במסמך.

**בנייה:** שם קובץ חדש בכל פעם (ה-DLL ננעל), חמישה טוקנים לעדכן, ו**ארבע** הפניות `/r:` —
`acmgd` · `acdbmgd` · `accoremgd` · `ProStructuresNet` · **`PSN_HollowShapeBracing`** (קל לפספס).

⚠️ **הבאש כאן בולע בקסלאשים ונשבר על גרשים** — כל סקריפט פייתון לכתוב לקובץ ולהריץ.
⚠️ **אל תעביר קובץ UTF-8 דרך `Get-Content`/`Set-Content` בלי `-Encoding` בשני הצדדים** — זה
שיבש את העברית ב-`eb_api.py` פעם אחת.
⚠️ **אסור `git stash` כש-AutoCAD מחזיק את ה-DWG** — git לא מצליח לשחרר את הקובץ וה-pop נכשל.
הסדר שעובד: **קומיט → `pull --rebase` → push**.

---

## מה הלאה

**חלק B סגור, והתחנה הבאה תיקבע על ידי אמיר.** (11/08/2026 — *"אגיד לך כשנחזור ונמשיך לעבוד."*)

⛔ **אין להתחיל אף אחד מהבאים בלי שהוא יאמר.** רשומים כדי שהמיפוי לא יילך לאיבוד:

- **חלק E — המוצרים של ארץ ברזל.** B.23 כבר מיפה מה נגיש: מעקות, סולמות, מסגרות פורטל
  ו-bracings **כן** ניתנים לתסריט; מדרגות, ג'ויסטים, טרסים וגאסטים **לא**.
- **שרשרת הפלט** (B.29.2/3/6/7 + חלק C) — שאמיר דחה במפורש עד שהמודל נעול.
- **ארבע ההחלטות שלמעלה** — בהולד.

<div dir="rtl" align="right">

# 🔒 איך משייכים לסוכן מודל — המדריך של אמיר

*נקבע 17/08/2026, לפי שתי הסיטואציות שאמיר הגדיר. **שתיהן נבדקו חיות מקצה לקצה באותו יום** —
סיטואציה 1: ‏6/6 שערים · סיטואציה 2: ‏4/4 שערים · מבחן המנעולים: ‏43/43.
המדידות והמנגנון: `api+knowledge-develop/knowledge/learning/findings/PARALLEL-MODELS.md`.*

---

## 🟢 הדרך הפשוטה: להגיד לסוכן בצ'אט. זהו.

**אתה לא חייב להריץ אף פקודה.** הסוכן יודע להריץ את כולן בעצמו. כל מה שצריך זה משפט אחד
בצ'אט, והוא כבר ינעל את עצמו וידווח לך שהוא נעול.

| מה אתה רוצה | המשפט שאתה כותב לסוכן בצ'אט |
|---|---|
| לשייך אותו למודל אחד | **"אתה עובד רק על המודל `<שם או נתיב>` — נעל את עצמך אליו. המשימה: `<מה לעשות>`"** |
| לוודא שהוא נעול | **"תראה לי verify"** |
| לשחרר אותו | **"שחרר את הנעילה"** |
| כמה מודלים (סיטואציה 2) | **"אתה עובד על שלושת המודלים האלה בלבד: ‏1) `<מודל>` — `<משימה>` · ‏2) `<מודל>` — `<משימה>` · ‏3) `<מודל>` — `<משימה>`. נעל את עצמך אליהם."** |

הפקודות בהמשך הדף הן למקרה שתרצה להריץ **בעצמך**, מחוץ לצ'אט — הן עושות בדיוק את אותו הדבר.

> ⚠️ **על הדוגמאות בדף הזה:** בכל מקום שכתוב `<נתיב למודל>` — מחליפים בנתיב האמיתי של הפרויקט.
> ‏(בגרסה הקודמת של הדף הופיע נתיב של שיעור 7 — זו הייתה **סתם דוגמה**, לא חלק מהפקודה.)

---

## 🎯 סיטואציה 1 — הסוכן על מודל אחד, אתה חופשי לגמרי (המצב של עכשיו)

**מה שביקשת:** *"אני נותן לסוכן לעבוד על פרויקט ומקשר אותו למודל שיעבוד רק עליו. במקביל אני
עובד על מודל אחר שאני פותח, או סוגר ופותח אחר, והוא לא מתערב לי."*

### הצעדים, אחד-אחד

**צעד 1 — תגיד לסוכן בצ'אט:**

> אתה עובד רק על המודל `<נתיב למודל>` — נעל את עצמך אליו. המשימה: `<המשימה>`.

הסוכן יריץ `assign`, יריץ `verify`, וידווח לך: *"נעול על `<המודל>`, זו המשימה, זה מה שפתוח."*

**צעד 2 — אתה פותח AutoCAD משלך ועובד על מה שבא לך.**

- פותח שרטוט, סוגר, פותח אחר — **חופשי לגמרי, בלי להודיע ובלי לרשום כלום.**
- שני התנאים היחידים:
  1. ‏🕐 **ה-AutoCAD שלך נפתח אחרי של הסוכן** (הסוכן קודם — אתה שני).
  2. ‏🚫 **לא פותחים את השרטוטים שלך בתוך החלון של הסוכן** — פותחים אותם בחלון שלך.

**צעד 3 — אין צעד 3.** עובדים. כשהסוכן מסיים — אומרים לו "שחרר את הנעילה" (או נותנים לו מודל חדש).

### למה זה בטוח — מה שנמדד היום

| מה נבדק | התוצאה |
|---|---|
| ה-AutoCAD שלך (שנפתח שני) | **בלתי-נראה לסוכן.** הוא פיזית לא מסוגל לשלוח לו כלום — נמדד: `hidden: 1` |
| הסוכן ממשיך לעבוד בזמן שאתה עובד | ✅ עבד ברצף |
| הסוכן מנסה לגעת במודל שלך | `EB_ERR assigned` — סירוב, שום דבר לא בוצע |
| סגרת AutoCAD ופתחת אחר | לסוכן לא זז כלום |

### ⛔ הטעות האחת שיכולה לתקוע — ואיך מזהים אותה

אם ה-AutoCAD **שלך** נפתח **לפני** של הסוכן — אז דווקא **שלו** הופך לבלתי-נגיש, וכל פקודה
שלו תיכשל עם:

```
EB_ERR unreachable: ... 1 further AutoCAD process(es) are running that COM CANNOT reach
```

שום נזק לא נגרם — אבל הסוכן משותק. **הפתרון:** סוגרים את אחד משני ה-AutoCAD ומתחילים בסדר
הנכון: קודם הסוכן, אחר כך אתה.

---

## 🎯 סיטואציה 2 — כמה מודלים, משימה שונה לכל אחד (לקראת ההשקה במשרד)

**מה שביקשת:** *"אני אתן לסוכן כמה מודלים במקביל ולכל אחד הנחיות שונות, והוא יעבוד על כולם
ויסנכרן בין כל משימה למודל שלה."*

**כן — זה בדיוק מה שנבנה, וזה כבר עובד.** נמדד היום: שני מודלים בו-זמנית, כל משימה צמודה
למודל שלה, כל פעולה נחתה במודל הנכון (99 ישויות מול 732 — אין ערבוב).

### הצעדים

**צעד 1 — תגיד לסוכן בצ'אט (משפט אחד עם הרשימה):**

> אתה עובד על שלושת המודלים האלה בלבד — נעל את עצמך אליהם:
> ‏1) `<נתיב מודל א>` — המשימה: `<...>`
> ‏2) `<נתיב מודל ב>` — המשימה: `<...>`
> ‏3) `<נתיב מודל ג>` — המשימה: `<...>`

**צעד 2 — `verify` מראה לך את כל הסט:**

```
ASSIGNED : 🔒 2 models:
           D-miscellaneous.dwg          task: לתקן את הרצועה
           E-structural-elements.dwg    task: לבדוק את המדרגות
WILL USE : D-miscellaneous.dwg
MATCH    : ✅ the agent's model IS the one in front
```

**צעד 3 — הסוכן עובד על כולם, ואתה מוגן מכל הכיוונים:**

- כל פעולה נושאת את זהות המודל שלה, וכל מודל מקבל את הפלטים **לתיבת דואר משלו** — אין ערבוב מדידות.
- מודל **מחוץ לרשימה** — סירוב מיידי: `EB_ERR assigned: the agent is locked to 'D…', 'E…'`.
- ⚠️ חשוב לדעת: "במקביל" = **לסירוגין** — ‏AutoCAD מריץ פקודה אחת בכל רגע. שלושת המודלים
  מתקדמים באותו סשן; הם לא רצים ממש בו-זמנית.

---

## 🖥️ הפקודות עצמן — אם אתה רוצה להריץ בעצמך

*רצות מ-`C:\Users\User\Desktop\EB PROSTEEL AGENT`. עושות בדיוק מה שהסוכן עושה כשאתה מבקש בצ'אט.*

**לנעול על מודל אחד:**

```bash
python "api+knowledge-develop/app/worksession.py" assign "<נתיב למודל>" --task "<המשימה>"
```

**לנעול על כמה (סיטואציה 2) — פשוט מוסיפים נתיבים:**

```bash
python "api+knowledge-develop/app/worksession.py" assign "<מודל א>" "<מודל ב>" "<מודל ג>"
```

**לוודא — שואל את AutoCAD עצמו, לא את הרישום:**

```bash
python "api+knowledge-develop/app/worksession.py" verify
```

**לשחרר:**

```bash
python "api+knowledge-develop/app/worksession.py" unassign
```

**ואם בכל זאת בא לך לפתוח שרטוט שלך בתוך ה-AutoCAD של הסוכן** — תכריז עליו קודם, והוא לא ייגע:

```bash
python "api+knowledge-develop/app/worksession.py" claim "<הנתיב לשרטוט שלך>"
```

---

## 👁️ איך קוראים את הפלט של `verify`

| השורה | מה היא אומרת |
|---|---|
| **ASSIGNED** | על מה נעלת אותו. `—` = לא נעול, חופשי לבחור |
| **WILL USE** | לאן תלך הפעולה הבאה **בפועל**. ⭐ השורה הקובעת |
| **ACTIVE IN AUTOCAD** | מה בחזית עכשיו — **נקרא מהתוכנה החיה**, לא מקובץ |
| **MATCH** | ✅ = תואם · ⚠️ = הסוכן יביא את המודל שלו לחזית לפני הפעולה הבאה |
| **`held by agent, ASSIGNED`** | מודל של הסוכן, בתוך הנעילה |
| **`not registered`** | שרטוט שאיש לא רשם — הסוכן **לא ייגע בו ולא יחליף ממנו תצוגה** |

## 🛡️ חמשת המנעולים (כולם נבדקו — כל סירוב ירה בכוונה, 43/43)

| # | המנעול | מה הסוכן מדפיס כשזה יורה |
|---|---|---|
| 1 | **נעילת הקצאה** — מודל מחוץ לרשימה שלך | `EB_ERR assigned: the agent is locked to '…'` |
| 2 | **בעלות** — מודל שהכרזת עליו `claim` | `EB_ERR hands-off: '…' is held by owner=amir` |
| 3 | **שרטוט לא רשום בחזית** — לא מחליף תצוגה | `EB_ERR blocked: … not registered to anyone` |
| 4 | **תוקף** — נעילה מאתמול דורשת אישור מחדש | `EB_ERR stale session … Yesterday's pin is not today's consent` |
| 5 | **מופע בלתי-נגיש** — ה-AutoCAD שלו מוסתר | `EB_ERR unreachable: … close that AutoCAD or close this one` |

⭐ נעילת ההקצאה **גוברת על הכל** — אין דרך לעקוף אותה, לא בפקודה, לא בקוד, לא במשתנה סביבה.
רק `unassign` שלך משחרר.

</div>

<div dir="rtl" align="right">

# 🔒 עבודה על מודל מסוים — הנוהל של אמיר

*נקבע 17/08/2026. הקובץ הזה יושב ליד `SESSION-START.md` בכוונה: זה הדף היחיד שאתה מריץ ממנו
פקודות. **המדידות והמנגנון** נמצאים ב-`api+knowledge-develop/knowledge/learning/findings/PARALLEL-MODELS.md`
— כאן רק מה אתה עושה, ואיך אתה רואה בעיניים שזה עובד.*

---

## ⚡ שלוש הפקודות שאתה צריך

כל הפקודות רצות מתיקיית הפרויקט (`C:\Users\User\Desktop\EB PROSTEEL AGENT`).

**‏1. להצמיד את הסוכן למודל אחד — והוא לא יוכל לגלוש לשום מודל אחר:**

```bash
python "api+knowledge-develop/app/worksession.py" assign "api+knowledge-develop/projects/lesson-7/lesson 7- Pump Chamber - REBUILD.dwg" --task "לסיים את הפלטות המכופפות"
```

**‏2. לוודא — הפקודה הזו שואלת את AutoCAD עצמו, לא את הרישום:**

```bash
python "api+knowledge-develop/app/worksession.py" verify
```

**‏3. לשחרר, כשרוצים להעביר אותו למודל אחר:**

```bash
python "api+knowledge-develop/app/worksession.py" unassign
```

---

## 👁️ מה `verify` מראה לך, ואיך קוראים את זה

```
ASSIGNED : 🔒 D-miscellaneous.dwg   (C:\...\projects\sandbox\D-miscellaneous.dwg)
WILL USE : D-miscellaneous.dwg
ACTIVE IN AUTOCAD: C:\...\projects\sandbox\E-structural-elements.dwg
MATCH    : ⚠️  different — הסוכן ימשוך את המודל שלו לחזית לפני האופ הבא, או יסרב

open drawings in this AutoCAD:
    D-miscellaneous.dwg          held by agent, ASSIGNED
  ▶ E-structural-elements.dwg    not registered
```

| השורה | מה היא אומרת |
|---|---|
| **ASSIGNED** | המודל שאתה נעלת. אם כתוב `—` — לא נעלת כלום והסוכן חופשי לבחור |
| **WILL USE** | המודל שהאופ הבא **באמת** ילך אליו. ⭐ **זו השורה שקובעת** |
| **ACTIVE IN AUTOCAD** | מה נמצא בחזית ברגע זה — **נקרא מהתוכנה**, לא מקובץ |
| **MATCH** | ✅ = שניהם אותו דבר. ⚠️ = הסוכן יביא את שלו לחזית לפני שיעבוד |
| **‏▶ ליד שרטוט** | זה מה שבחזית עכשיו |
| **`not registered`** | שרטוט שאף אחד לא הכריז עליו — הסוכן **לא יגע בו ולא יחליף ממנו תצוגה** |

---

## 🛡️ מה מונע ממנו לגלוש — ארבעה מנעולים, כולם מסרבים בקול

| המנעול | מתי הוא יורה | מה הסוכן מדפיס |
|---|---|---|
| **הקצאה** (`assign`) | הוא ניסה לגעת במודל אחר — **בכל דרך** | `EB_ERR assigned: the agent is locked to '…'` |
| **בעלות** (`claim`) | הוא ניסה לגעת במודל שהכרזת כשלך | `EB_ERR hands-off: '…' is held by owner=amir` |
| **שרטוט לא רשום** | בחזית יושב שרטוט שאינו שלו | `EB_ERR blocked: … not registered to anyone` |
| **תוקף** | משמרת מאתמול / מסשן אחר | `EB_ERR stale session … Yesterday's pin is not today's consent` |

⭐ **וגם:** הקצאה **גוברת על כל דרך עקיפה** — לא דרך פקודה, לא דרך משתנה סביבה, לא בקוד.
זה נבדק במפורש (`worksession_selftest.py`, ‏33/33).

---

## 🧭 שני מצבי עבודה — ומה נכון בכל אחד

### א. אתה נותן לסוכן מודל, ואתה לא נוגע ב-AutoCAD

זה המצב הפשוט. `assign` בתחילת העבודה, `verify` מתי שבא לך, `unassign` בסוף.

### ב. אתה רוצה לעבוד על מודל **בו-זמנית** איתו

⚠️ **חלון AutoCAD אחד מציג מסמך אחד בכל רגע.** אם השרטוט שלך פתוח **באותו** AutoCAD שבו הסוכן
עובד, אז ברגע שהוא ימדל — התצוגה תיקפץ למודל שלו. הוא לא יהרוס לך כלום (הוא מסרב לגעת), אבל
אתה תאבד את המסך.

⇒ **הדרך הנכונה: פתח AutoCAD משלך — אחרי שהסוכן כבר פתח את שלו.**

| למה דווקא "אחרי" | כי **רק מופע AutoCAD אחד נגיש ל-COM** — המוקדם שעדיין חי (נמדד 17/08). המופע שנפתח שני הוא **בלתי-נראה** לסוכן: לא פקודה, לא טעינת תוסף, לא שינוי משתנה. **החלון שלך שלך.** |
|---|---|

⛔ **המקרה ההפוך תקוע:** אם ה-AutoCAD **שלך** נפתח ראשון ושל הסוכן שני — שלו הוא המוסתר, וכל
אופ יסרב ב-`EB_ERR unreachable`. שום דבר לא ינחת במקום הלא-נכון, אבל גם שום דבר לא יעבוד עד
שתסגור אחד מהשניים.

**ואם בכל זאת נוח לך לעבוד באותו AutoCAD** — הכרז בעלות והוא יתרחק:

```bash
python "api+knowledge-develop/app/worksession.py" claim "<הנתיב לשרטוט שלך>"
```

ובסיום — `release "<הנתיב>"`.

---

## 📋 הרצף שאני ממליץ, כשאנחנו מתחילים לעבוד על פרויקט

1. אתה אומר לי בצ'אט על איזה מודל עובדים ומה המשימה.
2. אתה מריץ **`assign`** עם הנתיב — או אומר לי להריץ, ואז **`verify`** כדי לראות שזה נכון.
3. אני מדווח לך על מה אני עובד לפני האופ הראשון, ומכאן כל אופ נושא את זהות המודל.
4. בסוף העבודה על המודל — **`unassign`**, ואם סיימנו לגמרי גם `close`.

⚡ **וכלל שנשאר בתוקף:** כל מודל שנפתח נרשם. שרטוט שנפתח ולא נרשם — הסוכן מתייחס אליו כאל **שלך**
ולא ייגע בו. זה מכוון: *"לא סוגרים ולא מזיזים מה שלא פתחת."*

---

## 🧾 מה נבנה מאחורי הפקודות האלה (בקצרה)

- **משמרת עבודה לכל מודל** — נתיב מלא · בעלים · משימה · תוקף. החליפה פין ישן שהיה **ברירת מחדל
  ולא נעילה**, שלא פג תוקפו לעולם, והשווה **שם קובץ** בלבד.
- **תיבת דואר לכל מודל** (`v185`) — הפקודה, התוצאה וכל ~40 קובצי הפלט של האופים. קודם לכן הכל
  היה משותף למכונה כולה, ושתי עבודות במקביל יכלו לקרוא זו את המדידות של זו.
- **הקצאה** (`assign`) — המנעול שמונע גלישה, וזו התוספת שנולדה מהשאלה שלך.

📄 כל המספרים והמדידות: `api+knowledge-develop/knowledge/learning/findings/PARALLEL-MODELS.md`

</div>

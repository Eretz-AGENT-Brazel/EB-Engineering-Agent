---
name: bridge-model-rebuild
description: "מודל 7 — \"bridge model for amir\" של ברני, 21,737 ישויות: השחזור הסוגר של מסלול 2, לילה של 20/08/2026"
metadata: 
  node_type: memory
  type: project
  originSessionId: e1e7e219-55ad-4b68-8b41-c82b511ba272
  modified: 2026-08-20T15:36:03.467Z
---

**המודל האחרון בסדרת "למידת מודלים קיימים"** (חלק ה, מודלים 1–6 קדמו לו). אמיר, 20/08/2026:
*"זה בעצם הסגירה — המודל האחרון שאותו תשחזר 1:1 כמו שעשינו במודלים 1-6. רציתי שתראה מה זה
קובץ מדידה לפני"* ⇒ שיעור 8 (קבצי מדידה) היה **ההכנה** לזה בכוונה: ברני הכניס את המדידה
לתוך המודל, ולכן הקירות והמצב הקיים הם חלק מהגיאומטריה.

- **גשר**, מודל של **ברני** (מנהל החברה) · ‏`C:\Users\User\Desktop\bridge model for amir.dwg`
  ‏(10.2 MB) · ‏**21,737 ישויות** = ×15 מכל מה שקדם.
- ‏**7,807 לוחות · 6,240 פרופילים · 5,680 ברגים · 314 קירות `Ks_ConcretePanel` +
  ‏29 `ConcreteShape` + 15 `ConcreteSlab` + rebar · 26 פחים מכופפים · 7 `AcDb3dSolid` ·
  ‏1,235 מידות · 18 מכלולים.** העבודה הגיאומטרית: 4,477 מישורי חיתוך · 7,834 קיטומים ·
  ‏3,828 poly-cuts · 11,657 חורים (מהם **608 אובליים**) · 494 subBodies.
- השחזור: `api+knowledge-develop/projects/bridge-bernie/` (המקור מועתק עם `sha256` זהה,
  והבנייה ב-`… - REBUILD.dwg`; **לקובץ של ברני לא נכתב דבר**).
- 🌍 השכבות בתעתיק של ברני מספרות את המבנה: `MAAKE` (מעקה) · `SULAM GADOL/KATAN` (סולמות) ·
  `SHVAHA` (שבכה) · `KAIAM` (קיים) · `PC_BEAM`/`PC_COLUMN` (בטון) · `AXIS`.

⚡ **מה שהמודל הזה לימד, ומה שנשאר ממנו:** הכלים שנולדו כאן — [[bulk-ops-and-batch]] —
ושלוש עובדות שסותרות מה שהיה כתוב: ‏`at` של `plate9 mode=poly` הוא **ה-org**, לא מרכז
הקונטור · ‏`whoami` אינו שומר-מסמך · ‏`beam` **מחליף קטלוג בשקט** כששם החתך קיים במקום אחר
(431 חלקים יצאו מ-`DIN_FLACH` במקום `PANEL`). ‏🔩 וברגים: **`p1→p2` הוא ה-grip** (`KlemmLen`),
והציר בדאמפ הוא **האורך** — מתחילים מ-`p1`.

⚠️ **מה שלא נסגר, כדי שלא ייקרא כאילו כן:** השערים רצו ו**נכשלו** — ‏4 ירוקים בלבד
(‏0 יתומים · 6,240/6,240 פרופילים · 7,807/7,807 לוחות · מעטפה). ‏**המסירה היא
‏`… - REBUILD-3.dwg`** ולא `REBUILD.dwg` — כי **הרסתי 2,133 חורים בניסיון לתקן 1,098**
(המטריקה שלי הייתה ב-`lhm=1` והשער ב-`lhm=2`; ‏`killholefield` ואז קידוח שסירב בשקט) והתאוששתי
מהעתק שנלקח ב-19:01 **מסיבה אחרת**.
⭐⭐⭐ **לפני מעבר הרסני על מודל חי — העתק בכוונה. מזל אינו תוכנית גיבוי.**
⭐⭐ **ולאמת את המטריקה מול השער לפני שמייעלים אותה.**
⭐ ומה שכן עבד: **הזזת 793 לוחות ב-COM** (‏794 ← 1 בתוך 0.011 מ"מ) — כי **תיקון מזיז, לא
מוחק-ובונה**, ואחרי שיש מודיפיקציות מחיקה-ובנייה זורקת אותן.

**How to apply:** כשאמיר מוסר מודל להעתקה — לספור מחלקות **לפני** שבונים, ולבדוק שהקטלוגים
בכלל מותקנים כאן; מה שאין לו יוצר או קטלוג עובר ב-`xclone` **ומוצהר כמועבר**.
קשור: [[copy-from-the-model]] · [[per-project-not-universal]] · [[survey-files]]

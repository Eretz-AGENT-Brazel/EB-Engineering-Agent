# 🔁 פרומפט העברה — המשך ביקורת חלק B מפרק B.20

*נכתב 10/08/2026 בסוף סשן שהשלים B.1–B.19. להעתיק את הבלוק שבמסגרת לתוך סשן חדש.*

---

---

## ⚠️⚠️ קודם כל — מאיפה לפתוח את הסשן

**לפתוח את קלוד קוד על `C:\Users\User\Desktop` — לא על תיקיית הפרויקט.**

30 הזיכרונות של הפרויקט יושבים ב:

```
C:\Users\User\.claude\projects\C--Users-User-Desktop\memory\
```

שמקודד את **`C:\Users\User\Desktop`**. פתיחה על `...\Desktop\EB PROSTEEL AGENT` מפנה לתיקייה
אחרת (`C--Users-User-Desktop-EB-PROSTEEL-AGENT`) שבה **אין `memory` בכלל** — והסוכן יתחיל בלי
כלל הברזל, בלי הקריאות הקטלניות, בלי `no-written-standard`, בלי כלום.

⇒ **הפרויקט עצמו לא זז** — הוא ב-`C:\Users\User\Desktop\EB PROSTEEL AGENT`.
רק **נקודת הפתיחה** של קלוד קוד חייבת להיות ה-Desktop.

⚠️ **נבדק ולא הונח:** קיימות שבע תיקייות פרויקט, ורק שלוש מהן מחזיקות `memory`.
שלנו היא של ה-Desktop, עם 30 קבצים.

**בדיקה של 30 שניות:** אם הזיכרונות נטענו, הסוכן יודע מה כתוב ב-`MEMORY.md`.
אם לא — **לעצור ולהגיד לאמיר.**

## מה להדביק

```
המשך את הביקורת של חלק B במדריך ProStructures, מפרק B.20 ועד B.29.

קרא קודם:
  knowledge/learning/audits/AUDIT-PART-B-2026-08-10.md   ← 19 הפרקים שכבר נבדקו
  README.md                                              ← מפת הרשויות
  PROGRAM.md § חוק הרישום

לכל פרק, לפי הסדר, בלי לחזור אליי בין פרק לפרק:
  1. קרא את knowledge/learning/manual/B/MANUAL-NOTES-B<NN>-*.md
  2. מצא את הפער האמיתי — פריט פתוח, טענה שלא נמדדה, יכולת שנקראה ולא נבנתה
  3. תקן את זה במודל / בתוסף, ומדוד את התוצאה
  4. כתוב לרשומת הביקורת ולהערות הפרק
  5. הטמע בסקיל
  6. הרץ  python qc/consistency.py   ← חייב CLEAN
  7. קומיט ופוש

לאט לאט וביסודיות. אל תמהר.
```

---

## מה שהסשן החדש צריך לדעת מיד

**AutoCAD** — סגור בסוף הסשן. להפעיל כך (לא סתם `acad.exe` — הפרופיל הוא מה שטוען ProSteel):

```powershell
Start-Process 'C:\Program Files\Autodesk\AutoCAD 2015\acad.exe' -ArgumentList `
  '/p','"C:\Program Files\Bentley\ProStructures Ss6 R1\AutoCAD 2015\Prg\ProStructures_SS6.1ACAD_E001_409.arg"',`
  '/t','Ps191_Metric','/ld','ProStructuresLoader.arx' `
  -WorkingDirectory 'C:\Program Files\Bentley\ProStructures Ss6 R1\AutoCAD 2015\Dwg'
```

ואז לפתוח `projects/SANDBOX/B08-insert-shapes.dwg` **דרך COM גולמי** (`eb_api._app_doc()` מסרב
כשהשרטוט הלא-נכון פתוח), לסגור את `Drawing1`, ו-`NETLOAD` את ה-DLL שמופיע ב-`app/eb_api.py`.
הסקריפט המלא קיים ב-`knowledge/learning/findings/LETHAL-CALLS-do-not-invoke.md § Recovery`.

**גרסת התוסף** — **v156**, ותמיד לפי `app/eb_api.py` (`DLL` ו-`RUN_CMD`) ולא לפי מספר במסמך.
בנייה: שם קובץ חדש בכל פעם (ה-DLL ננעל), `csc.exe` דרך PowerShell.

**המודל** — `B08-insert-shapes.dwg`, שמור. רצועות הביקורת של היום ב-`y` שלילי, `x` 400000–162000.

---

## ⚠️ ארבעה דברים שיעלו מיד, ואסור לגלות אותם מחדש

1. **הבאש כאן בולע בקסלאשים ונשבר על גרשים.** כל סקריפט פייתון — לכתוב לקובץ עם הכלי הייעודי
   ולהריץ. לא `python -c`, לא heredoc.
2. **`connset` בהסגר** — הפיל את AutoCAD ארבע פעמים. אמיר אישר שיישאר בהסגר (10/08).
3. **שתי קריאות קטלניות** — `PsPlate.computeObjectWeigth`, `PsVolume.checkHoleEdgeDistance`.
   לפני כל מתודת `check*`/`compute*` לא מוכרת: `knowledge/learning/findings/LETHAL-CALLS-do-not-invoke.md`.
4. **`plate9 mode=rect` ממקם מ-`at=`**, לא `p1=`.

---

## הכללים שהיום ייצר, ושחייבים להישאר

> ⭐⭐ **פסק דין שווה רק כמו הבדיקה שהפיקה אותו.** ארבע מסקנות נמשכו ב-10/08, וכל אחת נקבה
> בעצמה בבדיקה החסרה ואז תויקה כממצא. **אם הערה מזכירה קריאה שלא בוצעה — זו משימה, לא ממצא.**

> ⭐ **שני ערכים שנותנים אותה ספירה אינם עושים אותו דבר עד שהמיקומים מסכימים.** הציל את
> B.14 ואת B.16.

> ⭐ **תכונת `String`/`Int32` שנראית כמו רשימה היא אינדקסר** — `get_X(int)`. ארבעה מקרים.
> הדאמפ לא יגיד; המהדר יגיד.

> ⭐ **כשמפריכים — לא מוחקים בשקט.** כותבים את ההפרכה במקום שבו הטענה ישבה, **ומוסיפים שורה
> ל-`qc/retracted.tsv`, בכל ניסוח ובשתי השפות.** טענה אינה משפט אלא משפחה של משפטים.

> ⭐ **סנכרון אינו כתיבה.** `sync.py` מגבה בלבד; `backup already current` לא אומר שהידע נשמר.

---

## מה שאמיר החליט ואסור לפתוח מחדש

| | |
|---|---|
| ⏸️ **שלב 2 נעול** | בחירת מחברים לפי עומס (DAST ב-B.17.2/B.19, Standard Definitions ב-B.18) — לדעת שקיים = מותר. להשתמש = לא |
| ⏸️ **`Duebel.mdb`** | *"תעבור למחברי קורות, אל תתעכב על זה"* |
| ⏸️ **מסדי המחברים** | שלב 1 (קריאה) בוצע. **שלבים 2–3 מוקפאים** — אין טבלת EB, אין כתיבה לתוכנה |
| ⏸️ **שלוש הצעות חלק A** | בהולד |
| ⏳ **ממתין להחלטה** | פער 31 מ"מ ב-B.25 · 12 ברגים לא-מורכבים ב-B.26 |

⚠️ **ולארץ ברזל אין תקן מחברים כתוב** — הכל מניסיון אמיר ולפי דרישת הפרויקט
(`memory/no-written-standard.md`). **אסור להמציא פרט ולקרוא לו "הסטנדרט של ארץ ברזל".**

---

## 🧲 כללי הברזל — לא משתנים

1. **בורג חייב לעבור דרך חור מדולל בכל אלמנט שהוא מחבר.** אין "חומר". החריג היחיד — בורג
   קודח, ורק בהצהרה מפורשת של אמיר לכל מקרה. **שתיקה = שגיאה קריטית.**
2. **LISP אסור.** 3. **מטרי תמיד.** 4. **לשמור את המודל אחרי כל שלב.**
5. **`sandbox.dwg` הוא של אמיר** — לא לשמור, לא לשנות, לא לסגור.
